-- To change states, return a string with the state name.
return {
    enter = function(state)
    end,
    update = function(state, dt)
        return "main"
    end,
    draw = function(state)
    end,
    exit = function(state)

    end,
}
